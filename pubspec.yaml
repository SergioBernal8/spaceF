name: space_f
description: Flutter Create App

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  rxdart: ^0.21.0
  cupertino_icons: ^0.1.2
  progress_indicators:
    git: https://github.com/SergioBernal8/progress_indicators.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

